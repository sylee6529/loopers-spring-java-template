## 1. 유비쿼터스 언어

- **`상품`(Product)**: 판매되는 개별 항목. (옵션/색상/사이즈 등은 고려하지 않음)
- **`브랜드`(Brand)**: 상품을 보유한 주체.
- **`좋아요`(ProductLike)**: 사용자가 상품에 남기는 찜 표시 (사용자 당 상품별 1회).
- **`주문`(Order)**: 여러 상품을 한 번에 구매한 기록.
- **`주문 항목`(OrderItem)**: 주문에 포함된 개별 상품과 수량, 당시 가격 정보.
- **`포인트`(Point)**: 충전해 두고 결제에 사용하는 잔액.
- **`쿠폰`(Coupon)**: 할인률이 포함된 할인 수단. (한 주문에 쿠폰 1장만 적용)
- **`카테고리`(Category)**: 상품을 분류하는 라벨.

## 2. 정책 정의

- **포인트**: 만료 기간을 두지 않고 잔액만 관리합니다.
- **쿠폰 적용 방식**: 한 주문당 쿠폰 1장만 적용되며, 주문 전체 금액을 대상으로 할인합니다.

  (특정 상품에만 다른 쿠폰을 각각 적용하는 기능은 제공하지 않습니다.)

- **결제 실패 시 쿠폰**: 결제가 실패해도 쿠폰은 사용 처리된 것으로 유지합니다.

## 3. 유스케이스 (사용 시나리오)

### 🧩 상품 목록 조회

- **흐름**
    1. 사용자는 상품 목록을 조회할 수 있다.
        - `브랜드`로 필터링, `최신순`/`가격순`/`좋아요`순 정렬 가능
        - 로그인한 유저의 경우, 나의 `좋아요` 여부를 함께 조회
- **예외**
    - 존재하지 않는 브랜드로 필터링하면, “`존재하지 않는 브랜드`입니다.”라는 예외 메시지가 보여진다.

### 🧩 상품 상세 조회

- **흐름**
    1. 사용자는 상품 상세를 조회할 수 있다.
        - 로그인한 유저의 경우, 나의 `좋아요` 여부를 함께 조회
- **예외**
    - 상품이 존재하지 않으면 “`현재 조회할 수 없는 상품입니다`.”라는 예외 메시지가 보여진다.
    - 현재 판매중인 상품이 아니면 “`판매중인 상품이 아닙니다.`”라는 예외 메시지가 보여진다.

### 🧩 상품 브랜드 정보 조회

- **흐름**
    1. 사용자는 브랜드 정보를 조회할 수 있다.
- **예외**
    - 브랜드가 존재하지 않으면 “`존재하지 않는 브랜드입니다`.”라는 예외 메시지가 보여진다.

---

### 🖤 내가 좋아요한 상품 목록

- **흐름**
    1. 로그인한 사용자는 “`좋아요`한 상품” 메뉴를 통해 사용자의 좋아요 표시한 상품 목록을 조회할 수 있다.
    - 각 항목에는 `상품정보`, `좋아요` 수, 나의 `좋아요` 여부(항상 True)가 함께 조회
- **예외**
    - 비로그인 사용자는 “`로그인한 사용자만 이용 가능합니다`.”라는 예외 메시지가 보여진다.

### 🖤  상품 좋아요 등록

- **흐름**
    1. 로그인한 사용자가 상품에서 `좋아요` 버튼을 누른다.
    2. 만약 이미 `좋아요`한 상태라면 화면상 변화가 없고, 아닌 경우 `좋아요`가 새로 반영된다.
    3. 상품의 `좋아요` 수가 업데이트된다.
- **예외**
    - 비로그인 사용자는 “`로그인한 사용자만 이용 가능합니다`.”라는 예외 메시지가 보여진다.
    - 상품이 존재하지 않으면 “`현재 조회할 수 없는 상품입니다`.”라는 예외 메시지가 보여진다.
    - 현재 판매중인 상품이 아니면 “`판매중인 상품이 아닙니다.`”라는 예외 메시지가 보여진다.

### 🖤 상품 좋아요 취소

- **흐름**
    1. 로그인한 사용자가 `좋아요` 취소를 누른다.
    2. 만약 이미 `좋아요`가 없는 상태라도 에러 없이 그대로 진행된다.
    3. 상품의 `좋아요` 수가 업데이트된다.
- **예외**
    - 비로그인 사용자는 “`로그인한 사용자만 이용 가능합니다`.”라는 예외 메시지가 보여진다.
    - 상품이 존재하지 않으면 “`현재 조회할 수 없는 상품입니다`.”라는 예외 메시지가 보여진다.
    - 현재 판매중인 상품이 아니면 “`판매중인 상품이 아닙니다.`”라는 예외 메시지가 보여진다.

---

### 🛒 주문 생성

- **흐름**
    1. 로그인한 사용자가 구매할 상품과 수량을 확인하고 주문을 진행한다.
    2. 시스템은 `재고`, (있다면) `쿠폰` 적용 가능 여부, `포인트` 잔액을 확인한다.
    3. 조건이 맞으면 `주문`을 생성하고, `포인트` 차감, `재고` 차감, `쿠폰` 사용 처리를 진행한다.
    4. `주문`이 완료처리 된다.
- **예외**
    - 일부 상품만 품절인 경우, 주문 전체가 실패로 처리된다.
    - 비로그인 사용자는 “`로그인한 사용자만 이용 가능합니다`.”라는 예외 메시지가 보여진다.
    - 재고 부족인 경우, “`상품 재고가 부족하여 구매할 수 없습니다.`”라는 예외 메시지가 보여진다.
    - 상품이 존재하지 않으면 “`현재 조회할 수 없는 상품입니다`.”라는 예외 메시지가 보여진다.
    - 현재 판매중인 상품이 아니면 “`판매중인 상품이 아닙니다.`”라는 예외 메시지가 보여진다.
    - 포인트 부족인 경우, “`포인트가 부족하여 구매할 수 없습니다.`”라는 예외 메시지가 보여진다.

### 🛒 내 주문 목록/상세 조회

- **흐름**
    1. 로그인한 사용자가 주문 목록 또는 특정 주문 상세를 연다.
  2. 본인의 주문한 각 주문이 조회되며, 당시의 가격 정보가 함께 표시된다.
